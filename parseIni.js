module.exports = function parseIni(content){
    const content = JSON.stringify(str)
    const spl = ('/=/g')
    let newContent = content.split(spl)

    for(i = 0; i < spl.length ; i++){
            let keys = ''
            let values = ''
        
    }

    const EnvJSON = JSON.parse(newContent )
}
